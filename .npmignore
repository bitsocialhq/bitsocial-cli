# oclif pack tarballs (legacy, no longer generated)
dist/*.tar.gz

# Downloaded web UIs (fetched at install time via postinstall)
dist/webuis/

# TypeScript incremental build caches
dist/*.tsbuildinfo
